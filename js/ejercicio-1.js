// Crea un vector vacio llamado colores, luego agrega tres colores diferentes y recorre el vector
let colores = [];
colores.push("Azul", "Rojo", "Verde");
for (let i=0; i < colores.length; i++){
    console.log(colores[i]);
}
